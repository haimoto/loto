# loto — 運用ルール

ロト6/7 の予測ツール（`loto_predictor_chatgpt.py`）と当選番号 CSV。`scripts/` に運用コマンドがある。

## 即実行コマンド（調べ直さない・スクリプトを書き起こさない）

| 言われたこと | 実行する |
|---|---|
| 「Safari で入力して」「数字選択して」「N口出して入れて」 | `python3 scripts/takarakuji_fill.py --loto loto7 --hitprob 10 --ev 5` |
| 「予測だけ出して」 | 上に `--dry-run` |
| 「ちゃんと入ってる？」「確認して」 | 上に `--verify` |
| 「ログインできてる？」「締切は？」 | 上に `--check` |
| 「CSV を最新にして」 | `docs/csv-update.md`（ソースは確定済み。探し直さない）→ `python3 scripts/verify_csv.py` |

**⚠️ 宝くじ公式サイトはセッションが切れる。** probe・関数調査・アドホックな AppleScript の
書き起こしに時間を使うと、入力し終える前にログアウトして全部無駄になる（2026-08-12 に実際に発生）。
**上のコマンドを即実行する。** 足りない機能があればスクリプト本体を直す。
purchase 操作（「カートに入れる」以降）には進まない — 数字選択まで。

## モードの使い分け

- **通常 = hitprob**（`--hitprob`）: 「N口中1口でも本数字3個以上」の確率を最大化。履歴非依存・決定論的。
  CSV を更新しても組は変わらない。組数ごとに最適構造が確定している（上限20口）。
- **1等狙い・配当優先 = ev**（`--ev`）: 配当分配最適化。履歴依存なので CSV 更新で変わる。
- 「的中率を上げたい」は、**5口合計の期待ヒット数なら数学的に不可能**、
  「≥3個を1口以上」なら上げられる、と峻別して説明する。
- `any3` は入賞率ではない（ロト7は本数字3個だけでは無入賞）。報告時に混同しない。

## 参照

- Safari 入力の詳細・落とし穴 → `docs/takarakuji-safari-input.md`
- CSV のデータソース・フォーマット・検証 → `docs/csv-update.md`
- テスト: `uv run --with pytest python -m pytest tests/ -q`（約70秒。hitprob の最適値ロックを含む）
